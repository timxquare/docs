{
  "name": "icemail-mcp",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Remote MCP server for Icemail — cold email infrastructure (mailboxes, domains, DNS) over the Model Context Protocol.",
  "scripts": {
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "deploy": "wrangler deploy",
    "cf-typegen": "wrangler types",
    "inspector": "npx @modelcontextprotocol/inspector"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.25.2",
    "agents": "^0.3.10",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260706.1",
    "@types/node": "^26.1.1",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
